# Kodassery Diaries v2.0 asset intake

This is a read-only metadata intake for the seven supplied GLBs. It records file sizes and glTF JSON observations; it does not establish visual quality, licensing, suitability, or final in-game dimensions. All actual scale, license status, and visual approval remain unknown.

## Source inventory

| Source path | Size | glTF contents observed | Key risk / use note |
|---|---:|---|---|
| `public/assets/cars/bronco.glb` | 0.31 MB | 6 nodes, 2 meshes, 2 materials; no animations | No wheel/tire-named nodes. Its two generic materials do not yet establish body-only recoloring; tire motion requires geometry separation or a derived asset. |
| `public/assets/cars/car_carton.glb` | 11.07 MB | 24 nodes, 11 meshes, 3 materials; no animations | Explicit `wheel_front_left_6`, `wheel_front_right_7`, `wheel_rear_left_8`, `wheel_rear_right_9` nodes. Strong candidate for name-based wheel animation. |
| `public/assets/cars/car.glb` | 3.30 MB | 80 nodes, 1 mesh, 1 material, 5 animations | Detailed `WheelFront.*`, `WheelBack.*`, and `Mec.Wheel*` hierarchy, but wheel geometry is under one mesh. Raw position accessor span is about `4.237 × 7.670 × 8.352`; this is not a confirmed transformed vehicle size. |
| `public/assets/cars/fennec_-_rocket_league_car.glb` | 7.68 MB | 21 nodes, 13 meshes, 7 materials; no animations | Four separable rim nodes (`Alpha - FR/FL/BR/BL`) and tread meshes; `Paint` material is useful for recoloring. Names need a dedicated animator mapping. |
| `public/park/amusement_park.glb` | 31.17 MB | 4,277 nodes, 1,739 meshes, 94 materials; no animations | Large colorful scene, but raw accessor bounds are extreme (about `1,211,725 × 164,275 × 1,216,990`). The cause is unknown from metadata alone: local meshes may use different scales, or hierarchy transforms may account for the span. Do not place as one normalized prop until the park geometry is isolated. |
| `public/assets/buildings/coffee_shop_isometric.glb` | 0.82 MB | 151 nodes, 79 meshes, 27 materials; no animations | Building, floor, furniture, and props are present. Building-only extraction should retain attached structural slabs/verandas where needed, while removing presentation planes and unwanted interior props. |
| `public/assets/buildings/low_poly_fuel_station.glb` | 24.63 MB | 165 nodes, 93 meshes, 33 materials; no animations | Contains station, asphalt/ground, buildings, cars, pumps, signs, rocks, and props. Raw accessor span is about `51.150 × 35.900 × 24.937`; isolate the station kit and decide explicitly whether its ground plane is retained. |

The raw spans above come from `POSITION` accessor min/max values. They are source-space metadata, not transformed world dimensions: node transforms, exported roots, rotations, and the runtime bounding-box normalization can materially change the usable size. `ModelAsset` currently normalizes vehicle models by bounds, so each vehicle still requires an explicit target length/height and rotation decision.

## Acceptance status

Acceptance is pending user visual review. No asset has been visually inspected in the running game, and no claim is made that the models match the approved Kerala circa-2000 art direction. Confirm placement, color treatment, collision footprint, and building/ground extraction with screenshots before calling any asset ready.

## Required optimization and extraction pass

1. For `amusement_park.glb`, inspect the scene hierarchy, identify the actual park root and intended rides/buildings, remove export debris and unrelated nodes, then generate a bounded placement wrapper. Keep the big Silver Storm pool as a separately authored water surface so its footprint and collision are controlled.
2. For `low_poly_fuel_station.glb`, isolate the station canopy, pumps, signs, and selected props; exclude duplicate cars and the asphalt presentation plane unless the world layout explicitly needs it. Preserve attached structural slabs or verandas. Normalize the extracted group before placement.
3. For `coffee_shop_isometric.glb`, retain the building shell, attached structural slab/veranda, and requested exterior identity while filtering presentation planes and interior-only furniture from the world prop.
4. For cars, add model IDs and per-model target dimensions/rotation, then map only verified wheel nodes. Treat Bronco tire motion as unresolved until geometry can be separated or a derived asset is produced.
5. Recheck draw-call/triangle cost after extraction, especially for the 31 MB park and 24.63 MB fuel station, and use distance-based loading or reduced variants if the running UI shows frame cost.

No production files were changed and no tests were run for this intake.

## V2-03 preparation follow-up — 15 September 2026

Terrain approved and committed as `cb09c95`. The later user asset commit `ad0ec4c` is preserved. Source GLBs remain unchanged by this preparation pass. Read-only Luna selector audit informed explicit extraction; core implemented clone-only normalization/material compatibility and `/v2-assets.html`. Luna added focused preparation tests. Original intake claims above describe the earlier metadata-only stage.

| Prepared review asset | Width × height × depth (m) | Meshes / triangles | Preparation and remaining issue |
|---|---|---|---|
| Coffee shop | 8.85 × 5.60 × 9.14 | 78 / 15,884 | Removed loader node `Plane003_61` (source `Plane.003_61`), retained structural `Piso_3`. Exterior furniture remains for review. |
| Fuel station | 14.92 × 5.20 × 13.27 | 67 / 17,499 | Removed presentation asphalt, car assemblies, extremely dense text and boundary fences. Replacement local signage belongs to world placement. Before text/fence removal the extracted kit was 576,183 triangles. |
| Park | 90.00 × 48.62 × 82.14 | 1,711 / 304,463 | Removed exported camera/navigation helpers and four 32 m deep boundary skirts. Still expensive and contains unrelated source-game props; not approved for placement. Foundations/base anchoring and curation remain. |
| Bronco | 2.49 × 2.21 × 4.40 | 2 / 4,222 | Rotate long axis to Z; preserve original texture palette. Body paint/tire separation remains unresolved. |
| Cartoon car | 2.35 × 1.94 × 3.80 | 11 / 76,606 | Static scale target only; wheel integration pending. |
| Rigged car | 1.91 × 3.46 × 3.80 | 1 / 26,923 | Unusually tall transformed source; axes/rig pose need visual review. Do not freeze collision yet. |
| Fennec | 2.06 × 1.47 × 4.00 | 13 / 84,823 | Rotate long axis to Z; static scale target only. |

Measurements use precise transformed Three.js bounds after extraction and uniform scaling, not raw accessor bounds. Node selectors fail explicitly if missing. The park and Bronco use legacy specular/gloss materials: a loader adapter preserves diffuse factors/textures and approximates roughness as nonmetallic PBR. This is not exact specular/gloss rendering or approved recoloring.

The yard loads one selected model at a time, exposes keyboard view buttons, shows a 1 m grid/post and 1.7 m reference figure, and disposes preview resources on switching. The reference figure is not final avatar art. All seven records remain `prototype` with unknown license in the manifest. Network sizes are not reduced by runtime extraction; derived optimized exports and production loading/collision still remain. No new models have been added to the playable scene or car picker.
